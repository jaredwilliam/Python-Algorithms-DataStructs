def cap_text(text):
    '''
    Input a string
    Output the capitalized string
    '''
    # capitalize() only impacts the first letter of a string
    # we can use title() to impact all individual words of a string
#     return text.capitalize()
    return text.title()